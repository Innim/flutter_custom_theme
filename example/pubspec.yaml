name: example
description: Example of custom theme usage.
version: 1.0.0+1

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_custom_theme:
    path: ../

dev_dependencies:
  innim_lint: ^0.1.7+1

flutter:
  uses-material-design: true
